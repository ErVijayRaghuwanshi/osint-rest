apiVersion: v1
kind: ConfigMap
metadata:
  name: osint-scraper-config
  namespace: osint
data:
  PORT: "8080"
  LOG_LEVEL: "info"
  proxy-cache-path: "/tmp/nginx-cache levels=1:2 keys_zone=osint-cache:10m inactive=60m max_size=1g"
